# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - nwf-report

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Log in to registry
      uses: docker/login-action@v1
      with:
        registry: https://nwfreport.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_3de9504216984779afb86806657d4762 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_ffb16db639f34d1a9cd6c63359ed911e }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v2
      with:
        push: true
        tags: nwfreport.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_3de9504216984779afb86806657d4762 }}/nwf-report:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'nwf-report'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_93e026f7838c4c7991694bddc55b2d0e }}
        images: 'nwfreport.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_3de9504216984779afb86806657d4762 }}/nwf-report:${{ github.sha }}'